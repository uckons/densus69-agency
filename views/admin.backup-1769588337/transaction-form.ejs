<div class="max-w-4xl mx-auto space-y-6">
    <!-- Header -->
    <div>
        <a href="/admin/transactions" class="text-admin-600 hover:text-admin-700 text-sm font-medium mb-2 inline-block">
            <i class="fas fa-arrow-left mr-2"></i>Back to Transactions
        </a>
        <h2 class="text-2xl font-bold text-gray-800">Add New Transaction</h2>
        <p class="text-gray-600 text-sm mt-1">Record a new transaction for a model</p>
    </div>
    
    <!-- Transaction Form -->
    <div class="bg-white rounded-xl shadow-md p-8">
        <form id="transaction-form" onsubmit="handleSubmit(event)">
            <div class="space-y-6">
                <!-- Select Model -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-user mr-2"></i>Select Model *
                    </label>
                    <select name="modelId" 
                            required
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500"
                            onchange="updateModelRate(this)">
                        <option value="">Choose a model...</option>
                        <% if (typeof models !== 'undefined' && models.length > 0) { %>
                            <% models.forEach(model => { %>
                                <option value="<%= model._id %>" data-rate="<%= model.rate || 0 %>">
                                    <%= model.name %> - <%= model.city %> (<%= model.status %>)
                                </option>
                            <% }) %>
                        <% } %>
                    </select>
                </div>
                
                <!-- Date -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-calendar mr-2"></i>Transaction Date *
                        </label>
                        <input type="date" 
                               name="date" 
                               required
                               value="<%= new Date().toISOString().split('T')[0] %>"
                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500">
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-briefcase mr-2"></i>Job Type *
                        </label>
                        <select name="jobType" 
                                required
                                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500">
                            <option value="">Select job type...</option>
                            <option value="Fashion Show">Fashion Show</option>
                            <option value="Photo Shoot">Photo Shoot</option>
                            <option value="Commercial">Commercial</option>
                            <option value="Event">Event</option>
                            <option value="Promotional">Promotional</option>
                            <option value="Video Shoot">Video Shoot</option>
                            <option value="Other">Other</option>
                        </select>
                    </div>
                </div>
                
                <!-- Client Info -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-building mr-2"></i>Client Name *
                        </label>
                        <input type="text" 
                               name="clientName" 
                               required
                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500"
                               placeholder="Client or company name">
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-phone mr-2"></i>Client Contact
                        </label>
                        <input type="tel" 
                               name="clientContact" 
                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500"
                               placeholder="+62 xxx xxxx xxxx">
                    </div>
                </div>
                
                <!-- Amount -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-dollar-sign mr-2"></i>Amount (IDR) *
                        </label>
                        <input type="number" 
                               id="amount"
                               name="amount" 
                               required
                               min="0"
                               step="1000"
                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500"
                               placeholder="0"
                               onchange="calculateCommission()">
                        <p class="text-xs text-gray-500 mt-1">Model's earning</p>
                    </div>
                    
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-percentage mr-2"></i>Agency Commission (%)
                        </label>
                        <input type="number" 
                               id="commission"
                               name="commission" 
                               min="0"
                               max="100"
                               value="20"
                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500"
                               placeholder="20"
                               onchange="calculateCommission()">
                        <p id="commission-amount" class="text-xs text-gray-500 mt-1">Commission: Rp 0</p>
                    </div>
                </div>
                
                <!-- Duration -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-clock mr-2"></i>Duration (Days)
                    </label>
                    <input type="number" 
                           name="duration" 
                           min="1"
                           value="1"
                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500">
                </div>
                
                <!-- Status -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-toggle-on mr-2"></i>Status *
                    </label>
                    <select name="status" 
                            required
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500">
                        <option value="pending">Pending</option>
                        <option value="completed" selected>Completed</option>
                        <option value="cancelled">Cancelled</option>
                    </select>
                </div>
                
                <!-- Description -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-align-left mr-2"></i>Description
                    </label>
                    <textarea name="description" 
                              rows="4"
                              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500"
                              placeholder="Additional notes about this transaction..."></textarea>
                </div>
                
                <!-- Payment Method -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        <i class="fas fa-credit-card mr-2"></i>Payment Method
                    </label>
                    <select name="paymentMethod" 
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-admin-500">
                        <option value="cash">Cash</option>
                        <option value="bank_transfer">Bank Transfer</option>
                        <option value="e-wallet">E-Wallet</option>
                        <option value="cheque">Cheque</option>
                    </select>
                </div>
            </div>
            
            <!-- Action Buttons -->
            <div class="flex justify-end space-x-4 mt-8 pt-6 border-t border-gray-200">
                <a href="/admin/transactions" 
                   class="px-6 py-3 border border-gray-300 rounded-lg text-gray-700 font-medium hover:bg-gray-50 transition">
                    Cancel
                </a>
                <button type="submit" 
                        class="px-6 py-3 bg-admin-600 text-white rounded-lg font-medium hover:bg-admin-700 transition">
                    <i class="fas fa-save mr-2"></i>Save Transaction
                </button>
            </div>
        </form>
    </div>
</div>

<script>
    function updateModelRate(select) {
        const selectedOption = select.options[select.selectedIndex];
        const rate = selectedOption.getAttribute('data-rate');
        
        if (rate && rate > 0) {
            document.getElementById('amount').value = rate;
            calculateCommission();
        }
    }
    
    function calculateCommission() {
        const amount = parseFloat(document.getElementById('amount').value) || 0;
        const commissionPercent = parseFloat(document.getElementById('commission').value) || 0;
        const commissionAmount = (amount * commissionPercent) / 100;
        
        document.getElementById('commission-amount').textContent = 
            `Commission: ${formatCurrency(commissionAmount)} (Total: ${formatCurrency(amount + commissionAmount)})`;
    }
    
    async function handleSubmit(event) {
        event.preventDefault();
        
        const form = event.target;
        const formData = new FormData(form);
        
        try {
            showLoading();
            const response = await fetch('/admin/transactions', {
                method: 'POST',
                body: formData
            });
            
            const data = await response.json();
            hideLoading();
            
            if (data.success) {
                showToast('Transaction created successfully', 'success');
                setTimeout(() => {
                    window.location.href = '/admin/transactions';
                }, 1500);
            } else {
                showToast(data.message || 'Failed to create transaction', 'error');
            }
        } catch (error) {
            hideLoading();
            showToast('An error occurred', 'error');
        }
    }
</script>
